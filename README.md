-- npm init -y 

--npm i express mongoose dotenv cookie-parser jsonwebtoken

-- npm i morgan

-- all secrets access in the config.js

-- npm i express-validator
    -- for validation of user information data

-- npm i bcryptjs
    -- in models to convert password